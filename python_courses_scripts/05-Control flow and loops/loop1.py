# Assume you have a list of 5 numbers. Write a script to compute the square of each number using for loop.

numbers = [1, 3, 5, 8, 12]
for i in numbers:
  square = i ** 2
  print("Square of:", i, "is:", square)

  
